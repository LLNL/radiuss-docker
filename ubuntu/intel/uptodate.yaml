dockerbuild:
  container_basename: radiuss

  matrix:
    intel_version:
      - "2025.2.1"
    ubuntu_version:
      - "24.04"

  build_args:
    intel_version:
      key: intel
      name: intel

    ubuntu_version:
      key: ubuntu
      name: ubuntu
