dockerbuild:
  container_basename: radiuss

  matrix:
    intel_version:
      - "2023.1.0"
      - "2024.0"
    ubuntu_version:
      - "20.04"
      - "20.04"

  build_args:
    intel_version:
      key: intel
      name: intel

    ubuntu_version:
      key: ubuntu
      name: ubuntu
