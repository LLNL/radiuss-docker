dockerbuild:

dockerbuild:
  container_basename: radiuss

  matrix:
    ubuntu_version:
       - "20.04"
       - "20.04"
    hip_version:
       - "5.6.1"
       - "6.0.2"
       
  build_args:
    hip_version:
      key: hip
      name: hip

    ubuntu_version:
      key: hip
      name: hip