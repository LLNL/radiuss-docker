dockerbuild:
  container_basename: radiuss

  matrix:
    llvm_version:
      - "19"
    ubuntu_version:
      - "24.04"

  build_args:
    llvm_version:
      key: clang
      name: ubuntu

    ubuntu_version:
      key: ubuntu
      name: ubuntu
